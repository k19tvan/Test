# Problem 08 Theory

## Integral Formula
$$y = \int_0^{\infty} p(x) \cdot W(x) dx \approx \sum_{i=0}^{n} p_i \cdot w_i$$

where $p_i = \sigma(\text{dist})$ (softmax) and $w_i$ is the weighting function value.
